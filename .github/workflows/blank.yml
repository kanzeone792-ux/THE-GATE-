<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>THE GATE</title>
  <link rel="stylesheet" href="styles.css"/>
</head>
<body>
  <header class="hero">
    <div class="hero-content">
      <h1>THE GATE</h1>
      <p>The Growing Africans Through Entrepreneurship and Enterprises</p>
      <a href="#contact" class="cta-button">Join Our Movement</a>
    </div>
  </header>

  <section class="about">
    <h2>Business Mentorship for Rising Entrepreneurs</h2>
    <p>At THE GATE, we mentor aspiring African entrepreneurs to build sustainable businesses through practical advice, strategy, and enterprise growth support.</p>
  </section>

  <section class="contact" id="contact">
    <h2>Contact Us</h2>
    <p>Ready to grow your business?</p>
    <ul>
      <li>📞 0615760621</li>
      <li>📧 <a href="mailto:kanzeonever792@gmail.com">kanzeonever792@gmail.com</a></li>
    </ul>
  </section>

  <footer>
    <p>© 2025 THE GATE. Empowering Entrepreneurs Through Mentorship.</p>
  </footer>
</body>
</html> This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
body {
  margin: 0;
  font-family: 'Segoe UI', sans-serif;
  background-color: #fffaf0;
  color: #333;
}

.hero {
  background: linear-gradient(to right, #ffd700, #ffcc00);
  color: #000;
  text-align: center;
  padding: 100px 20px;
}

.hero h1 {
  font-size: 3em;
  margin-bottom: 10px;
}

.hero p {
  font-size: 1.2em;
}

.cta-button {
  background-color: #000;
  color: #ffd700;
  padding: 12px 24px;
  margin-top: 20px;
  display: inline-block;
  text-decoration: none;
  border-radius: 5px;
}

.about, .contact {
  padding: 60px 20px;
  max-width: 800px;
  margin: 0 auto;
}

.about h2, .contact h2 {
  color: #b8860b;
  margin-bottom: 10px;
}

.contact ul {
  list-style: none;
  padding: 0;
  font-size: 1.1em;
}

.contact a {
  color: #b8860b;
  text-decoration: none;
}

footer {
  text-align: center;
  padding: 20px;
  background: #333;
  color: white;
}
